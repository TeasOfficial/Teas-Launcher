[workspace]
resolver = "2"
members = [
    "src-tauri",
]

[workspace.package]
edition = "2021"

# 体积优化: opt-level="s" + LTO + strip
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
